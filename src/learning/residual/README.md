# src/learning/residual

Trạng thái: **removed from active pipeline** theo `DEC-044` ngày 2026-08-29.

Không triển khai hoặc train bounded residual correction NN. Thư mục này chỉ
giữ provenance của quyết định thiết kế; paper bước 5 vẫn là literature archive.

`Prediction residual delta x` trong feature surrogate/confidence là one-step
plant/model mismatch signal và không thuộc module đã bỏ.
