# Common Controller Helpers

Shared MATLAB helpers for NMPC rollout, cost, bounds, references and warm starts.

`nmpc_control_horizon.m` and `nmpc_expand_control_sequence.m` implement move blocking for `Nc <= N`.
